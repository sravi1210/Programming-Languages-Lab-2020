# To Compile And Run The Program Use:
	$ swipl -s problem3.pl

# For Querying If A Path Is Valid Use:
	?- valid([Path]). 

# For Querying To Find All The Paths Use:
	?- paths(). 
		Output is stored in the output.txt File.

# For Querying The Optimal Path Use:
	?- optimal().

